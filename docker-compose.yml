services:
  speedwatch:
    build: .
    container_name: speedwatch
    ports:
      - "3001:3001"
    volumes:
      - speedwatch-data:/app/backend/data
    environment:
      - PORT=3001
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  speedwatch-data:
